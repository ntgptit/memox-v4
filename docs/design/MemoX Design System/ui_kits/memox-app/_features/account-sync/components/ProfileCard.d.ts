/**
 * Account-sync signed-in profile card: avatar + name + email + ALPHA badge.
 *
 * The kit renders this as a **static fixture** (hardcoded name/email) and exposes
 * **no props**. The account-sync screen is DEFERRED (WBS S.22) — there is no
 * Flutter counterpart yet, recorded as a `deferred-screen` exception.
 */
export interface ProfileCardProps {}

export function ProfileCard(): JSX.Element;
