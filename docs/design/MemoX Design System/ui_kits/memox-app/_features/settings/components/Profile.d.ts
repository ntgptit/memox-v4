/**
 * Settings profile card: avatar + name + email.
 *
 * The kit renders this as a **static fixture** (hardcoded name/email) and exposes
 * **no props**. The Flutter `ProfileCard` is likewise self-contained
 * (`const ProfileCard()`) — so both sides expose no props.
 */
export interface ProfileProps {}

export function Profile(): JSX.Element;
