# Account & Sync — UI contract (kit)

> **GENERATED** từ UI kit (nguồn chuẩn). Flutter PHẢI render đủ & khớp các state dưới đây.
> Screen `account-sync` · pushed/overlay · 5 state.

## ⊘ v1: HOÃN

Kit thiết kế màn Account & Sync, nhưng **v1 HOÃN** (không backend từ xa). Giữ spec để tham khảo; không build ở v1.

## States (bắt buộc khớp kit)

- `conflict`
- `offline`
- `signed-in`
- `signed-out`
- `syncing`

## Nguồn chuẩn

- DOM spec (authoritative): [`specs/account-sync.md`](../design/MemoX%20Design%20System/ui_kits/memox-app/specs/account-sync.md)
- Wireframe (ảnh từng state): [`../wireframes/account-sync.md`](../wireframes/account-sync.md)
- Shots: `docs/design/MemoX Design System/ui_kits/memox-app/shots/account-sync--*.png`
