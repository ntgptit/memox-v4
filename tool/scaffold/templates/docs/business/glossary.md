# Glossary — {{PROJECT_NAME}}

The canonical name for every domain concept. Define a term once here; reuse the
exact word everywhere (code, docs, UI copy). Renaming a term means updating all
refs — find them with `node tool/doc_guard/run.mjs terms <old>`.

| Term | Definition | Notes |
| --- | --- | --- |
| <Term> | <one-sentence definition> | <enum values / synonyms to avoid> |

<!-- FILL: every noun in the domain. Ambiguous language is where drift starts. -->

## Related

- `docs/business/index.md` — where these terms are used
