# Where-is: feature → docs / source / tests / WBS

Read the relevant row BEFORE grepping. Keep one row per feature; update it in the
same commit that adds/moves a feature.

| Feature | Business doc | Source | Tests | WBS |
| --- | --- | --- | --- | --- |
| <example> | `docs/business/<area>/<feature>.md` | `{{SRC_DIR}}/...` | `test/...` | <WBS-ID> |

<!-- FILL: one row per feature. This is the fastest way to onboard a new session. -->
