/// The four practice games (`docs/contracts/types-catalog.md`,
/// `docs/business/game/game-modes.md`). Practice only — never changes the
/// schedule (D-007).
enum GameType { matching, multipleChoice, recall, typing }
