import 'package:memox_v4/app/bootstrap.dart';

/// The app's single entrypoint. All wiring (ProviderScope, error zone, router,
/// theme) lives in [bootstrap] / [MemoxApp] so `main` stays a one-liner.
void main() => bootstrap();
