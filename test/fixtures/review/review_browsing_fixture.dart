// review · browsing — golden-parity fixture.
import '../_fixture.dart';
import '_review_harness.dart';

/// Browsing a card — term, meaning, position.
final StateFixture reviewBrowsingFixture = StateFixture(
  overrides: roundOverrides(),
);
