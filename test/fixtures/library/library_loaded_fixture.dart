// library · loaded — golden-parity fixture.
import '../../harness/provider_harness.dart';
import '../_fixture.dart';

/// The seeded deck tree.
final StateFixture libraryLoadedFixture = StateFixture(
  overrides: FakeHarness().overrides,
);
