// study-session · stage4-recall — golden-parity fixture (docs/agent/golden-parity/WBS.md).
import '../_fixture.dart';
import '_study_session_harness.dart';

/// New-learn stage 4: recall the meaning, then reveal to self-check.
final StateFixture studySessionStage4RecallFixture = StateFixture(
  overrides: studySessionStateOverrides(studySessionStage4),
);
