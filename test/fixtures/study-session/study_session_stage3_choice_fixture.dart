// study-session · stage3-choice — golden-parity fixture (docs/agent/golden-parity/WBS.md).
import '../_fixture.dart';
import '_study_session_harness.dart';

/// New-learn stage 3: pick the correct meaning from the choices.
final StateFixture studySessionStage3ChoiceFixture = StateFixture(
  overrides: studySessionStateOverrides(studySessionStage3),
);
