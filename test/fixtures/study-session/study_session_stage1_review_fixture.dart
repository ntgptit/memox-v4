// study-session · stage1-review — golden-parity fixture.
import '../_fixture.dart';
import '_study_session_harness.dart';

/// New-learn stage 1 (review) — the card + Next.
final StateFixture studySessionStage1ReviewFixture =
    StateFixture(overrides: studySessionNewOverrides());
