// study-session · stage5-typing — golden-parity fixture (docs/agent/golden-parity/WBS.md).
import '../_fixture.dart';
import '_study_session_harness.dart';

/// New-learn stage 5: type the term → card graduates into box 1.
final StateFixture studySessionStage5TypingFixture = StateFixture(
  overrides: studySessionStateOverrides(studySessionStage5),
);
