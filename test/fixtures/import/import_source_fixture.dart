// import · source — golden-parity fixture.
import '../../harness/provider_harness.dart';
import '../_fixture.dart';

/// Choose-source step, continue disabled.
final StateFixture importSourceFixture =
    StateFixture(overrides: FakeHarness().overrides);
