// game-typing · waiting — golden-parity fixture.
import '../_fixture.dart';
import '_game_typing_harness.dart';

/// Meaning shown, empty input, Check disabled.
final StateFixture gameTypingWaitingFixture = StateFixture(
  overrides: gameRoundOverrides(),
);
