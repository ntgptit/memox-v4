// export · config — golden-parity fixture.
import '../../harness/provider_harness.dart';
import '../_fixture.dart';

/// Export config — scope, format, separator, sub-decks toggle.
final StateFixture exportConfigFixture = StateFixture(
  overrides: FakeHarness().overrides,
);
