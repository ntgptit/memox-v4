// player · playing — golden-parity fixture.
import '../_fixture.dart';
import '_player_harness.dart';

/// Auto-play running — dots, card, transport with pause icon.
final StateFixture playerPlayingFixture = StateFixture(
  overrides: roundOverrides(),
);
