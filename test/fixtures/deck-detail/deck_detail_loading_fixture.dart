// deck-detail · loading — golden-parity fixture.
import '../_fixture.dart';
import '_deck_detail_harness.dart';

final StateFixture deckDetailLoadingFixture = StateFixture(
  overrides: deckDetailLoadingOverrides(),
);
