// deck-detail · empty — golden-parity fixture.
import '../_fixture.dart';
import '_deck_detail_harness.dart';

final StateFixture deckDetailEmptyFixture = StateFixture(
  overrides: deckDetailEmptyOverrides(),
);
