// deck-detail · error — golden-parity fixture.
import '../_fixture.dart';
import '_deck_detail_harness.dart';

final StateFixture deckDetailErrorFixture = StateFixture(
  overrides: deckDetailErrorOverrides(),
);
