// settings · loaded — golden-parity fixture.
import '../../harness/provider_harness.dart';
import '../_fixture.dart';

/// The settings hub — profile card + grouped rows.
final StateFixture settingsLoadedFixture = StateFixture(
  overrides: FakeHarness().overrides,
);
