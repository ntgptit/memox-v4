// theme · light — golden-parity fixture.
import '../../harness/provider_harness.dart';
import '../_fixture.dart';

/// Theme screen — preview + accent + text-size controls (seed-independent).
final StateFixture themeLightFixture = StateFixture(
  overrides: FakeHarness().overrides,
);
