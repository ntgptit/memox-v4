// drawer · open — golden-parity fixture.
import '../../harness/provider_harness.dart';
import '../_fixture.dart';

/// The drawer panel open with nav items + today's activity.
final StateFixture drawerOpenFixture = StateFixture(
  overrides: FakeHarness().overrides,
);
