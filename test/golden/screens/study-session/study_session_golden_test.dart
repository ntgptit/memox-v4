// GENERATED by tool/golden/scaffold.mjs — golden test (create-if-missing; safe to edit after generation).
@Tags(['golden-parity'])
library;

import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:memox_v4/core/theme/app_theme.dart';
import 'package:memox_v4/presentation/features/study-session/screens/study_session_screen.dart';
import '../../../fixtures/study-session/study_session_fixtures.dart';
import '../../support/screen_golden.dart';

/// Screen goldens for study-session — one per state × theme, driven by the
/// state fixtures. Tagged 'golden-parity' (excluded from the default gate;
/// a dedicated CI job owns the baselines). Un-filled fixtures fail fast.
void main() {
  final themes = <String, ThemeData>{
    'light': AppTheme.light,
    'dark': AppTheme.dark,
  };
  studySessionFixtures.forEach((state, fixture) {
    themes.forEach((theme, data) {
      testWidgets('study-session--$state--$theme', (tester) async {
        await pumpScreenGolden(
          tester,
          home: const StudySessionScreen(),
          fixture: fixture,
          theme: data,
        );
        await expectLater(
          find.byType(MaterialApp),
          matchesGoldenFile(
            '../../goldens/screens/study-session--$state--$theme.png',
          ),
        );
      });
    });
  });
}
