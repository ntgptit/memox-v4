// GENERATED by tool/golden/scaffold.mjs — golden test (create-if-missing; safe to edit after generation).
@Tags(['golden-parity'])
library;

import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:memox_v4/core/theme/app_theme.dart';
import 'package:memox_v4/presentation/features/player/screens/player_screen.dart';
import '../../../fixtures/player/player_fixtures.dart';
import '../../support/screen_golden.dart';

/// Screen goldens for player — one per state × theme, driven by the
/// state fixtures. Tagged 'golden-parity' (excluded from the default gate;
/// a dedicated CI job owns the baselines). Un-filled fixtures fail fast.
void main() {
  final themes = <String, ThemeData>{
    'light': AppTheme.light,
    'dark': AppTheme.dark,
  };
  playerFixtures.forEach((state, fixture) {
    themes.forEach((theme, data) {
      testWidgets('player--$state--$theme', (tester) async {
        await pumpScreenGolden(
          tester,
          home: const PlayerScreen(),
          fixture: fixture,
          theme: data,
        );
        await expectLater(
          find.byType(MaterialApp),
          matchesGoldenFile('../../goldens/screens/player--$state--$theme.png'),
        );
      });
    });
  });
}
